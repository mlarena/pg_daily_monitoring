# PostgreSQL Daily Monitoring Queries

Набор SQL-запросов для ежедневного мониторинга производительности и состояния PostgreSQL баз данных.

## 📋 О проекте

Этот проект содержит коллекцию SQL-запросов, предназначенных для регулярного мониторинга:
- Производительности БД
- Использования ресурсов
- Активности подключений
- Состояния репликации
- Эффективности индексов
- Процессов обслуживания

## 🚀 Быстрый старт

### Предварительные требования

1. **Включите расширение pg_stat_statements** в `postgresql.conf`:
```ini
shared_preload_libraries = 'pg_stat_statements'
pg_stat_statements.track = all
pg_stat_statements.max = 10000
```

2. **Создайте расширение** в вашей БД:
```sql
CREATE EXTENSION IF NOT EXISTS pg_stat_statements;
```

3. **Настройте логирование** медленных запросов:
```ini
log_min_duration_statement = 1000
```

## 📊 Категории запросов

### 1. 📈 Основные метрики БД
- Размеры баз данных и таблиц
- Количество подключений
- Hit ratio кэша

### 2. 🔍 Активность и производительность
- Активные сессии
- Медленные запросы
- Статистика по таблицам

### 3. ⚠️ Проблемы и блокировки
- Блокирующие сессии
- Долгие транзакции
- Взаимные блокировки

### 4. 📑 Индексы и оптимизация
- Неиспользуемые индексы
- Статистика использования индексов
- Отсутствующие индексы

### 5. 🔄 Репликация (если используется)
- Статус репликации
- Лаги репликации
- Статус слотов репликации

### 6. 🧹 Техническое обслуживание
- Мониторинг автовакуума
- Статистика WAL
- Контрольные точки

## 🛠 Использование

### Ежедневные проверки

```sql
-- Основной отчет о состоянии
SELECT * FROM daily_health_check;

-- Проверка медленных запросов
SELECT * FROM slow_queries_report;

-- Мониторинг репликации
SELECT * FROM replication_status;
```


## 📈 Ключевые метрики для мониторинга

### Критические метрики
- **Cache hit ratio** > 99%
- **Active connections** < 80% от max_connections
- **Replication lag** < 1MB
- **Dead tuples** < 10% от live tuples

### Предупреждающие метрики
- Медленные запросы (> 1 секунды)
- Неиспользуемые индексы
- Долгие транзакции (> 5 минут)
- Высокий процент dead tuples

## 🔧 Рекомендации по настройке

### Автовакуум
```sql
-- Для таблиц с высокой частотой изменений
ALTER TABLE table_name SET (
    autovacuum_vacuum_scale_factor = 0.05,
    autovacuum_analyze_scale_factor = 0.02
);
```

### Мониторинг
- Запускайте проверку блокировок каждые 5 минут
- Ежедневно проверяйте размеры БД
- Еженедельно анализируйте статистику запросов

## ⚠️ Важные примечания

1. Некоторые запросы требуют прав суперпользователя
2. Запросы могут оказывать нагрузку на систему
3. Рекомендуется запускать в часы низкой нагрузки
4. Регулярно обновляйте статистику с помощью `ANALYZE`

---

**Примечание**: Адаптируйте запросы под вашу версию PostgreSQL и конкретные требования к мониторингу.
```
